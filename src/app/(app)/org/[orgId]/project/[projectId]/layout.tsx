/**
 * Phase 3: Project scope under org. No extra sync; projectId is read from path by useRouteScope().
 */
export default function ProjectLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
