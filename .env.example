# LangGraph / Reflexion backend configuration
# Local: use proxy (port 8080). Do NOT use the Docker/backend URL on Railway — see note below.
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NEXT_PUBLIC_ASSISTANT_ID=agent
# Do NOT prefix this with "NEXT_PUBLIC_" as we do not want this exposed in the client.
LANGSMITH_API_KEY=

# Server-side: Next.js API routes proxy to the backend using LANGGRAPH_API_URL.
# Local Docker: backend is at backend:8080 or host.docker.internal:8080
# Railway: MUST set LANGGRAPH_API_URL to the backend's PUBLIC URL (e.g. https://reflexion-staging.up.railway.app).
#          Do not use localhost:8080 or backend:8080 on Railway — the frontend runs in a different service.
# LANGGRAPH_API_URL=http://backend:8080
# LANGGRAPH_API_URL=https://reflexion-staging.up.railway.app

# Production (e.g. Railway) — Uncomment and set to your deployment URLs
# NEXT_PUBLIC_API_URL=https://reflexion-ui-staging.up.railway.app/api
# LANGGRAPH_API_URL=https://reflexion-staging.up.railway.app
# LANGSMITH_API_KEY="lsv2_..."
